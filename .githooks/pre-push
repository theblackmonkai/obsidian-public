#!/bin/bash
echo "🔍 Scanning for personal info..."

# Check for actual secrets (not generic docs)
grep -riE "(sk-|ghp_|patr)" --include="*.md" . 2>/dev/null | grep -v ".git" && {
  echo "❌ Personal info detected! Aborting push."
  exit 1
}

echo "✅ No personal info detected"
exit 0
